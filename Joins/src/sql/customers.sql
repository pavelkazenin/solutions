create table training.customers (
    customer_id     integer,
    first_name      varchar(16),
    last_name       varchar(16),
    street          varchar(16),
    city            varchar(16),
    state           varchar(16),
    zip             varchar(10)
    )
